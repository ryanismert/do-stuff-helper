# W28: Auto-Dispatch Ready Work

## Objective
Automatically start executing planned waypoints when their dependencies are met.

## Done When
An n8n workflow or webhook hook detects waypoints in planned status with unblocked dependencies and triggers waypoint-implement automatically.
